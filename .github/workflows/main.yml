name: Docker Publish
on: workflow_dispatch
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Set up kubectl
        uses: centosadmin/kubernetes-helm
        with:
          version: '3.6.3'

      - name: Get nodes
        run: |
          kubectl get nodes
